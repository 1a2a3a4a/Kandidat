<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
android:layout_width="match_parent"
android:layout_height="match_parent">

<TextView
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="Opponent"
    android:id="@+id/opponentNameTextView"
    android:gravity="center_horizontal"
    android:maxLines="1"
    android:maxLength="25"
    android:layout_alignParentTop="true"
    android:layout_toEndOf="@+id/scoreTextView" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/courseNameTextView"
        android:maxLines="1"
        android:maxLength="15"
        android:ellipsize="end"
        android:layout_alignParentTop="true"
        android:layout_toStartOf="@+id/scoreTextView"
        android:text="Course" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="ytuyt"
        android:id="@+id/scoreTextView"
        android:layout_below="@+id/opponentNameTextView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="31dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Your score"
        android:id="@+id/userScoreTextView"
        android:layout_alignTop="@+id/scoreTextView"
        android:layout_toStartOf="@+id/courseNameTextView" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Opponents score"
        android:id="@+id/opponentScoreTextView"
        android:layout_alignBottom="@+id/scoreTextView"
        android:layout_toEndOf="@+id/opponentNameTextView" />

</RelativeLayout>
